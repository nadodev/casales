@extends('layouts.admin') @section('title','Avaliações') @section('content')
<div class="flex items-end justify-between"><div><span class="eyebrow">Conteúdo</span><h1 class="text-5xl">Avaliações</h1></div><a class="btn-primary" href="{{ route('admin.testimonials.create') }}">Nova avaliação</a></div>
<div class="mt-8 grid gap-4">@forelse($items as $item)<article class="card"><div class="flex flex-wrap justify-between gap-4"><div><p class="text-gold-600">{{ str_repeat('★',$item->rating) }}</p><h2 class="mt-2 text-2xl">{{ $item->name }}</h2><p class="text-sm text-muted">{{ $item->context }}</p><p class="mt-3">{{ $item->content }}</p></div><div class="flex items-start gap-3"><a class="font-bold text-green-700" href="{{ route('admin.testimonials.edit',$item) }}">Editar</a><form method="post" action="{{ route('admin.testimonials.destroy',$item) }}">@csrf @method('DELETE')<button class="font-bold text-red-700" onclick="return confirm('Remover esta avaliação?')">Excluir</button></form></div></div><p class="mt-4 text-xs font-bold uppercase {{ $item->is_active?'text-green-700':'text-muted' }}">{{ $item->is_active?'Publicada':'Oculta' }}</p></article>@empty<p>Nenhuma avaliação cadastrada.</p>@endforelse</div>@endsection
