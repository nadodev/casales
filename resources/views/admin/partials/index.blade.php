@extends('layouts.admin') @section('title',$title) @section('content')<div class="flex flex-wrap items-end justify-between gap-4"><div><span class="eyebrow">Conteúdo do site</span><h1 class="text-5xl">{{ $title }}</h1></div><a class="btn-primary" href="{{ $createUrl }}">Novo cadastro</a></div><div class="mt-8 overflow-x-auto rounded-2xl border bg-white"><table class="w-full text-left"><thead class="bg-beige-100"><tr><th class="p-4">Nome</th><th class="p-4">Status</th><th class="p-4 text-right">Ações</th></tr></thead><tbody>@forelse($items as $item)<tr class="border-t"><td class="p-4"><strong>{{ $item->{$nameField} }}</strong>@if($subtitleField && $item->{$subtitleField})<br><span class="text-sm text-muted">{{ $item->{$subtitleField} }}</span>@endif</td><td class="p-4">{{ $item->is_active ? 'Publicado' : 'Oculto' }}</td><td class="p-4"><div class="flex justify-end gap-2"><a class="rounded-lg border px-3 py-2" href="{{ route($routePrefix.'.edit',$item) }}">Editar</a><form method="post" action="{{ route($routePrefix.'.destroy',$item) }}" data-confirm="Tem certeza que deseja remover este item?">@csrf @method('DELETE')<button class="rounded-lg border border-red-200 px-3 py-2 text-red-700">Excluir</button></form></div></td></tr>@empty<tr><td class="p-8 text-center text-muted" colspan="3">Nenhum cadastro encontrado.</td></tr>@endforelse</tbody></table></div>@endsection
