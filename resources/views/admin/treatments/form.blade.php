@extends('layouts.admin') @section('title',$item->exists?'Editar tratamento':'Novo tratamento') @section('content')<h1 class="text-5xl">{{ $item->exists?'Editar tratamento':'Novo tratamento' }}</h1><form class="admin-form mt-8" method="post" action="{{ $item->exists?route('admin.treatments.update',$item):route('admin.treatments.store') }}">@csrf @if($item->exists)@method('PUT')@endif<div class="grid gap-5 md:grid-cols-2"><label>Nome<input class="field" name="name" value="{{ old('name',$item->name) }}" required></label><label>Slug<input class="field" name="slug" value="{{ old('slug',$item->slug) }}" required pattern="[a-z0-9-]+"></label><label>Categoria<input class="field" name="category" value="{{ old('category',$item->category) }}" required></label><label>Ordem<input class="field" type="number" min="0" name="sort_order" value="{{ old('sort_order',$item->sort_order??0) }}"></label></div><label>Resumo<textarea class="field" rows="3" name="excerpt" required>{{ old('excerpt',$item->excerpt) }}</textarea></label><label>Descrição<textarea class="field" rows="7" name="description" required>{{ old('description',$item->description) }}</textarea></label><label>Benefícios <span class="text-sm text-muted">(um por linha)</span><textarea class="field" rows="5" name="benefits_text">{{ old('benefits_text',implode("\n",$item->benefits??[])) }}</textarea></label><label class="flex items-center gap-3"><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$item->exists?$item->is_active:true))> Publicado no site</label><div class="flex gap-3"><button class="btn-primary">Salvar</button><a class="btn-secondary" href="{{ route('admin.treatments.index') }}">Cancelar</a></div></form>@endsection
