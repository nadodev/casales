@extends('layouts.admin') @section('title','Mensagens de contato') @section('content')
<span class="eyebrow">Atendimento</span><h1 class="text-5xl">Mensagens de contato</h1>
<div class="mt-8 grid gap-4">@forelse($items as $item)<a class="card block {{ $item->read_at ? 'opacity-75' : 'border-gold-600' }}" href="{{ route('admin.contact-messages.show',$item) }}"><div class="flex flex-wrap items-start justify-between gap-3"><div><h2 class="text-2xl">{{ $item->name }}</h2><p class="mt-1 text-sm text-muted">{{ $item->email }} @if($item->phone) · {{ $item->phone }}@endif</p><p class="mt-3 font-semibold">{{ $item->subject ?: 'Contato pelo site' }}</p></div><span class="text-sm text-muted">{{ $item->created_at->format('d/m/Y H:i') }}</span></div><p class="mt-3 text-muted">{{ Str::limit($item->message,180) }}</p>@if(!$item->read_at)<span class="mt-4 inline-block rounded-full bg-gold-600 px-3 py-1 text-xs font-bold text-white">Nova</span>@endif</a>@empty<p class="card">Nenhuma mensagem recebida.</p>@endforelse</div><div class="mt-8">{{ $items->links() }}</div>@endsection
