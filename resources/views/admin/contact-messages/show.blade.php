@extends('layouts.admin') @section('title','Mensagem de '.$item->name) @section('content')
<a class="font-bold text-green-700" href="{{ route('admin.contact-messages.index') }}">← Voltar às mensagens</a><article class="card mt-6"><span class="eyebrow">Recebida em {{ $item->created_at->format('d/m/Y H:i') }}</span><h1 class="text-4xl">{{ $item->subject ?: 'Contato pelo site' }}</h1><div class="mt-6 grid gap-2 border-y border-green-900/10 py-5"><p><strong>Nome:</strong> {{ $item->name }}</p><p><strong>E-mail:</strong> <a class="text-green-700 underline" href="mailto:{{ $item->email }}">{{ $item->email }}</a></p>@if($item->phone)<p><strong>Telefone:</strong> {{ $item->phone }}</p>@endif</div><div class="mt-6 whitespace-pre-line text-lg">{{ $item->message }}</div><div class="mt-8 flex flex-wrap gap-3"><a class="btn-primary" href="mailto:{{ $item->email }}?subject={{ rawurlencode('Re: '.($item->subject ?: 'Contato pelo site')) }}">Responder por e-mail</a><form method="post" action="{{ route('admin.contact-messages.destroy',$item) }}">@csrf @method('DELETE')<button class="btn-secondary" onclick="return confirm('Excluir esta mensagem?')">Excluir</button></form></div></article>@endsection
