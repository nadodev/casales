@extends('layouts.site') @section('title',$treatment->name.' em Piracicaba | Casale Saúde Integrada') @section('description',$treatment->excerpt) @section('content')<section class="section"><div class="container-site max-w-4xl"><span class="eyebrow">{{ $treatment->category }}</span><h1>{{ $treatment->name }}</h1><p class="mt-6 text-xl text-muted">{{ $treatment->excerpt }}</p><div class="mt-10 whitespace-pre-line text-lg">{{ $treatment->description }}</div>@if($treatment->benefits)<div class="mt-12 grid gap-4 sm:grid-cols-2">@foreach($treatment->benefits as $benefit)<div class="card font-semibold">✓ {{ $benefit }}</div>@endforeach</div>@endif<div class="mt-12 rounded-2xl bg-green-900 p-8 text-white"><h2 class="text-3xl">Converse com nossa equipe</h2><p class="mt-3 text-white/75">Entenda se este atendimento é adequado para você.</p><a class="btn mt-6 bg-beige-300 text-green-900" href="{{ route('contact') }}">Entrar em contato</a></div></div></section>@endsection
