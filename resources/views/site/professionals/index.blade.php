@extends('layouts.site') @section('title','Profissionais | Casale Saúde Integrada') @section('content')<section class="section"><div class="container-site"><span class="eyebrow">Nossa equipe</span><h1>Profissionais</h1><p class="mt-5 max-w-2xl text-muted">Conheça quem une experiência, escuta e cuidado individual.</p><div class="mt-12 grid gap-8 md:grid-cols-3">@foreach($professionals as $p)<article class="card p-0 overflow-hidden"><img src="{{ str_starts_with($p->image_path ?? '','images/') ? asset($p->image_path) : Storage::url($p->image_path) }}" width="600" height="760" alt="{{ $p->name }}" class="aspect-[4/5] w-full object-cover"><div class="p-6"><h2 class="text-3xl">{{ $p->name }}</h2><p class="mt-2 text-muted">{{ $p->title }}</p><p class="mt-4">{{ $p->summary }}</p><a class="mt-5 inline-block font-bold text-green-700" href="{{ route('professionals.show',$p) }}">Perfil completo →</a></div></article>@endforeach</div></div></section>@endsection
