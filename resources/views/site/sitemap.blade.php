{!! '<'.'?xml version="1.0" encoding="UTF-8"?'.'>' !!}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"><url><loc>{{ route('home') }}</loc></url><url><loc>{{ route('professionals') }}</loc></url>@foreach($professionals as $p)<url><loc>{{ route('professionals.show',$p) }}</loc></url>@endforeach<url><loc>{{ route('treatments') }}</loc></url>@foreach($treatments as $t)<url><loc>{{ route('treatments.show',$t) }}</loc></url>@endforeach<url><loc>{{ route('contact') }}</loc></url></urlset>
