import '@fontsource-variable/manrope';
const menuButton=document.querySelector('[data-menu-button]');const nav=document.querySelector('#main-nav');if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=menuButton.getAttribute('aria-expanded')==='true';menuButton.setAttribute('aria-expanded',String(!open));nav.classList.toggle('hidden',open);nav.classList.toggle('absolute',!open);nav.classList.toggle('left-0',!open);nav.classList.toggle('top-20',!open);nav.classList.toggle('w-full',!open);nav.classList.toggle('bg-surface',!open);nav.classList.toggle('p-5',!open);});}
const submenuButton=document.querySelector('[data-submenu-button]');const submenu=document.querySelector('#treatment-menu');if(submenuButton&&submenu){const close=()=>{submenu.hidden=true;submenuButton.setAttribute('aria-expanded','false');};submenuButton.addEventListener('click',()=>{const open=submenuButton.getAttribute('aria-expanded')==='true';submenu.hidden=open;submenuButton.setAttribute('aria-expanded',String(!open));});document.addEventListener('keydown',e=>{if(e.key==='Escape'){close();submenuButton.focus();}});document.addEventListener('click',e=>{if(!submenuButton.contains(e.target)&&!submenu.contains(e.target))close();});}
document.querySelectorAll('form[data-confirm]').forEach(form=>form.addEventListener('submit',e=>{if(!window.confirm(form.dataset.confirm))e.preventDefault();}));
if(!document.querySelector('link[rel="icon"]')){const favicon=document.createElement('link');favicon.rel='icon';favicon.type='image/svg+xml';favicon.href='/favicon.svg';document.head.append(favicon);}
